"""Synapic inference sidecar package.

Python inference engine served over a local FastAPI HTTP API, packaged with
PyInstaller as ``synapic-inference(.exe)`` (migration spec §4).
"""

__version__ = "1.0.0"
